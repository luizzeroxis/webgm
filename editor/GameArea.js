//
class GameArea {

	constructor(editor) {
		//TODO get room 0 size?
		parent( add( html('div', {class: 'game'}) ) )
			var canvas = add( newCanvas("canvas", 640, 480) )
			canvas.setAttribute('tabindex', 0);
			endparent()
	}
	
}